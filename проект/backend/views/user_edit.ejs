<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Edit User</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/public/main.css">
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center mb-4">Edit User</h1>

        <form action="/api/user/put/<%= user.IdClient %>" method="POST" >
            <div class="mb-3">
                <label for="IdClient" class="form-label">User ID</label>
                <input type="text" class="form-control" id="IdClient" name="IdClient" value="<%= user.IdClient %>" readonly>
            </div>
            <div class="mb-3">
                <label for="FullName" class="form-label">Full name</label>
                <input type="text" class="form-control" id="FullName" name="FullName" value="<%= user.FullName %>" required>
            </div>
            <div class="mb-3">
                <label for="Email" class="form-label">Email</label>
                <input type="text" class="form-control" id="Email" name="Email" value="<%= user.Email %>" required>
            </div>
            <div class="mb-3">
                <label for="Phone" class="form-label">Phone</label>
                <input type="number" class="form-control" id="Phone" name="Phone" value="<%= user.Phone %>" required>
            </div>


            <button type="submit" class="btn btn-primary">Update User</button>
        </form>

        <a class="btn btn-outline-secondary mt-3" href="/api/user" style="margin-left: 50px;">Back to Users</a>
    </div>
</body>
</html>